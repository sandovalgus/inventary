<%= simple_form_for [:taxes, @voucher_type], wrapper: :maxwell_horizontal_form, defaults: { grid_wrapper_html: { class: 'col-sm-9' } } do |f| %>
  <div class="card">
    <div class="card-header"><%= header %></div>

    <div class="card-body">
      <legend class="form-section">
        <i class="fa fa-mobile"></i> <%= t('.section.basic') %>
      </legend>

      <div class="form-group row string required">
        <%= f.input :name, as: :tel, wrapper: false, grid_wrapper_html: { class: 'col-sm-3' } %>
        <%= f.input :description, as: :tel, wrapper: false, grid_wrapper_html: { class: 'col-sm-3' } %>
      </div>
    </div>

    <div class="card-footer">
      <div class="float-right">
        <%= link_to t('.cancel'), taxes_voucher_types_path, class: 'btn btn-secondary' %>
        <%= f.button :submit, t('.submit'), class: 'btn btn-primary' %>
      </div>
    </div>
  </div>
<% end %>
 